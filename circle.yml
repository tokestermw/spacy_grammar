general:
  build_dir: .

machine:
  python:
    version: 3.6.3
  python-pip:
    version: 9.0.1

dependencies:
  post:
    - pip install pipenv
    - pipenv install

test:
  override:
    - pipenv run python -m spacy_grammar.grammar
