typos:
  as_follow_as_follows:
    description: 
    patterns: 
      # match directly on the string
      - as follow
      # alternatively, you can use spaCy attributes to match on each token
      -
        - LOWER: as
        - LOWER: follow
    corrections:
      - Do you mean "as follows"?
    examples: 
      - We can elaborate this distinction as follow.
